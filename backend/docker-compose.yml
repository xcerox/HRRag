services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: hrrag
      POSTGRES_USER: hrrag
      POSTGRES_PASSWORD: hrrag
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
